<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Appenders>

        <!-- Console output -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="[%d{HH:mm:ss}] [%-5level] [%t] [%c{1}] - %msg%n"/>
        </Console>

        <!-- Rolling file appender -->
        <RollingFile name="FileLogger"
                     fileName="logs/framework.log"
                     filePattern="logs/framework-%d{yyyy-MM-dd-HH-mm}-%i.log.gz">

            <PatternLayout>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5p [%c{1}] %m%n</pattern>
            </PatternLayout>

            <Policies>
                <SizeBasedTriggeringPolicy size="5MB" />
                <TimeBasedTriggeringPolicy />
            </Policies>

        </RollingFile>

    </Appenders>

    <Loggers>

        <!-- Framework package logging -->
        <Logger name="com.framework" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
        </Logger>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
        </Root>

    </Loggers>

</Configuration>
